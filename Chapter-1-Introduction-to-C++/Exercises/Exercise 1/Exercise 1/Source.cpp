#include <iostream>
#include <string> // to make string work
using namespace std;

int main() 
{
    int firstNumber = 1; // if whole number then integer (int)
    bool iCanCode = true; // if true or false then apply bool
    char hopefulGrade = 'a'; // any character can be used, and char instead of string because of ''
    double myDecimal = 1.0; // if decimal then apply double
    string minimalSentance = "y"; // string to assign it as written inside the ""
    int keyMash = 13213123; // whole number always int
    float mysteryDataType = 5.6f; // f made it float, without f it should be double

    return 0;
}